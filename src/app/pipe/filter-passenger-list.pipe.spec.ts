import { FilterPassengerListPipe } from './filter-passenger-list.pipe';

describe('FilterPassengerListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPassengerListPipe();
    expect(pipe).toBeTruthy();
  });
});
